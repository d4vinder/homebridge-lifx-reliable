/**
 * The platform name users register in their Homebridge config.json `platform` field.
 */
export const PLATFORM_NAME = 'LifxReliable';

/**
 * Must match the `name` field in package.json.
 */
export const PLUGIN_NAME = 'homebridge-lifx-reliable';
